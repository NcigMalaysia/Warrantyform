# Apps Script reference

The warranty form was restored to its version before the claim confirmation and browser storage changes. The original form already includes `claimId` in each POST, so the updated `doPost` in `Code.gs` can continue to receive its submissions. No new Apps Script deployment is required for this form rollback.

`Code.gs` is retained as a reference for the server code previously prepared for the owner. It is not automatically deployed to the owner's Google Apps Script project.
